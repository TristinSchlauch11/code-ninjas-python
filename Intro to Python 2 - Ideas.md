Structure of this document was copied from the original *Ideas to Start* file. Adjustments to the content of the course will be updated accordingly.

# Things to teach:

### Creating Actors (Basics and Variables)
- variable assignment structure (var = value)
- what a string is (for the image file)
- using these together to create an Actor
- update certain characteristics about the Actor, like position and image

### Handling Inputs (Input and Booleans)
- what is a Boolean
- if statements
- connectors (AND, OR, NOT)
- use these to handle certain inputs like mouse clicks and button presses
- set different buttons and mouse clicks to change the game in different ways